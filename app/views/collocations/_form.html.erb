
<%= form_with model: @collocation, local: true do |f| %>

    <%= f.label :title %>
    <%= f.text_field :title %>
    <br><br>

    <%= f.label :introduction %>
    <br>
    <%= f.text_area :introduction, size: "50*5" %>
    <br><br>

    <%= f.label :image, 'Image URL' %>
    <%= f.text_field :image %>
    <br><br>

    <%= f.label :garment_id, 'Must-Haves Items' %>
    <br>
    <%= f.collection_select :garment_id, Garment.all, :id, :name, include_blank: true %>
    <br><br>
    <%= f.collection_select :garment_id, Garment.all, :id, :name, include_blank: true %>
    <br><br>

    <%= f.submit %>

<% end %>

